 # keys in the datetime node list. An extra key in       
+ the datetime node map is                           
332 +  # silently ignored and the query is still built c      
+orrectly.                


 The pattern across many of these "fallback" functions              
  (resolve_source, having_expr, update_expr) is defensive dead code —
   they exist to prevent FunctionClauseError but the upstream query
  binding system guarantees only valid patterns. The most impactful  
  reachable lines involve: (1) through-associations with no queryable
   key, (2) passing non-standard types (DynamicExpr, tuple,          
  non-keyword-list) where string/atom types are expected, and (3)    
  fallback catch-all build_query clauses for unusual input types.  

Standardize error handling. The convert_params_to_filter pipeline
throughout shouldn't raise if the error is due to user errors in
the arguments, instead it should log a warning and skip the
operation. Things like configuration errors should raise.


setup the tests in a way that i can test postgres seperate from mysql

  ★ Insight ─────────────────────────────────────                                                                                                          
  The having.ex and or_having.ex catch-all *_expr lines at line 60 require a selected_binding value that doesn't match any of the 12 generated binding     
  patterns. Since QueryBinding generates patterns for {:as, nil}, {:as, name}, and {:at, 1..10}, only a {:at, 11} or higher would reach the catch-all — and
   CommonFilters rejects out-of-range positional bindings before dispatch.                                                                                 
  ─────────────────────────────────────────────────                          